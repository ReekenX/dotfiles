# This script I have created to quickly reinstall Operating system
#
# If you reinstalling your OS, before proceeding, have you:
#
#   * Copied database?
#   * Copied passwords database (Firefox, Opera, Keepass)?
#   * Copied bookmarks from Firefox, Opera, Chromium?
#   * Tared all files in /var/www/?
#   * Tared all files in /home/?
#   * Tared all files in /etc/apache2/?
#   * Tared all files in /etc/php5/?
#   * Tared all files in /usr/local/bin/?
#
# If you sure about this, let's reinstall and install all required software.
# This auto file will do everything all you need.
#
# Copyright (c) 2009-2010 Remigijus Jarmalavičius
# Remigijus Jarmalavičius <remigijus (eta) jarmalavicius (dot) lt>

# Update repository
sudo apt-get update;

# Upgrade Operating System to get latest software
sudo apt-get update;

# Install Apache2
sudo apt-get install apache2;

# Install PHP5
sudo apt-get install php5 libapache2-mod-php5;
sudo apt-get install php5-curl php5-gd php5-imagick php5-imapphp5-mcrypt php5-mhash php5-xmlrpc php5-xdebug;
echo "<?php phpinfo(); ?>" >> /var/www/phpinfo.php;
# NOTE: open http://localhost/phpinfo.php and test is PHP5 is working

# Install Mysql server
sudo apt-get install mysql-server;
sudo apt-get install libapache2-mod-auth-mysql php5-mysql;

# NOTE: change post_max_size and upload_max_filesize in php.ini minimum 500 MB (to
# uploaded large databases backups)

# Enable mod_rewrite
sudo a2enmod rewrite;
sudo /etc/init.d/apache2 restart;

# Mostly used programs
sudo apt-get install vim vim-full;
sudo apt-get install gedit gedit-plugins;
sudo apt-get install filezilla;
sudo apt-get install unzip;
sudo apt-get install htop;
sudo apt-get install alltray;

sudo apt-get install kchmviewer;
# NOTE: download PHP reference CHM from: http://php.net/download-docs.php
# NOTE: download Mysql reference CHM from: http://dev.mysql.com/doc/

# Install Java to support Open Office
sudo apt-get install openoffice.org-java-common;

# Install entertainment programs
sudo apt-get install vlc;
sudo apt-get install k3b;
sudo apt-get install gmountiso;
sudo apt-get install apg;
sudo apt-get install parcellite;
sudo apt-get install rar;
sudo apt-get install gajim;
sudo apt-get install chromium-browser;
sudo apt-get install cryptkeeper;
sudo apt-get install qbittorrent;
sudo apt-get install gnucash;
sudo apt-get install gimp;
sudo apt-get install skype;
sudo apt-get install evolution;

# NOTE: Install codecs, Microsoft fonts and other stuff
sudo apt-get install ubuntu-restricted-extras;

sudo apt-get install sensors-applet;
# NOTE: add sensor applet to your taskbar

# Version systems
sudo apt-get install mercurial;
sudo apt-get install git-core gitk;
sudo apt-get install subversion;
sudo apt-get install bazaar;

# Files comparing tools
sudo apt-get install meld;
sudo apt-get install kdiff3;

sudo apt-get install keepassx;
# NOTE: after installing open Keepassx and in settings change:
#   * Yes - Show system tray icon
#   * Yes - Minimize to tray instead of taskbar

# NOTE: download FireFox plugins:
#   * Firebug, Firebug validator, Web developer toolbar, NoScript,
#   * Screengrab, FireCookie, Fast dial, ColorZilla, Delicious bookmarks,
#   * Lithuanian spellchecking dictionary, FirefoxNotify.
# NOTE: download flash player (just visit website with flash and you will receive flash player).

# NOTE: download Opera from:
#   * http://www.opera.com/browser/download/

# NOTE: uninstall all Ubuntu default games you have received

# NOTE: After installation, go to System >> Administration >> Hardware drivers and install required drivers.

# NOTE: After installation go to System >> Preferences >> Power Management Preferences and edit as you like.

# NOTE: After installation go to System >> Preferences >> Preferred Applications and edit as you like.

# NOTE: After installation, go to System >> Administration >> Computer Janitor and clean your OS.

# NOTE: System >> Administration >> Language Support >> add Lithuanian language.
# NOTE: Also edit your OpenOffice and Evolution mail spell checking configuration.

# Remove unuseful programs
sudo apt-get purge dolphin;
sudo apt-get purge empathy;
sudo apt-get purge brasero;
sudo apt-get purge ubuntuone-client;
sudo apt-get purge ibus;
sudo apt-get purge transmission-gtk transmission-common;

