name: please-rename-me
root: <%= ENV["PWD"] %>
startup_window: zsh

windows:
  - editor: vim
  - zsh:
  - server:
    - git up
    - bundle install
    - bundle exec rake db:migrate
    - foreman start
  - db:
    - bundle exec rails db
  - git: tig
