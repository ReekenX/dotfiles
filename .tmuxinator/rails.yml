name: please-rename-me
root: <%= ENV["PWD"] %>
startup_window: zsh

windows:
  - zsh: subl .
  - server:
    - git up
    - bundle install
    - bundle exec rake db:migrate
    - foreman start
  - db:
    - bundle exec rails db
  - git: tig
