name: blog
root: <%= ENV["PWD"] %>

windows:
  - server:
    - cd output
    - python -m SimpleHTTPServer
  - pelican:
    - bash
    - source /usr/local/bin/virtualenvwrapper.sh
    - workon jarmalavicius.lt
    - pelican content
  - editor: vim
  - zsh:
  - git: tig
