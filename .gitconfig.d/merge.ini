[merge]
    log = true
    conflictstyle = diff3
    tool = vimdiff

[mergetool]
    prompt = false
    keepBackup = false

[mergetool "vimdiff"]
    cmd = nvim -d $LOCAL $MERGED $REMOTE

[diff]
    tool = icdiff

[icdiff]
    options = --line-numbers

[difftool]
    prompt = false
