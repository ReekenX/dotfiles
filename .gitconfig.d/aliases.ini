[alias]
    co = checkout -f
    ci = !git commit -a -q -v
    qa = !git add --all -p
    pu = !"clear; git diff @{u}..; echo 'Will start pushing in...'; echo -n 3; sleep 0.5; echo -n .; sleep 0.5; echo -n .; echo -n 2; sleep 0.5; echo -n .; sleep 0.5; echo -n .; echo -n 1; sleep 0.5; echo -n .; sleep 0.5; echo '.Go! '; for r in `git remote`; do echo "Pushing to remote: $r"; git push -q $r 2>&1 | sed 's/^/    /'; done;"
    df = !"PATH=$PATH:.gitconfig.d/bin/icdiff git icdiff"
    lg = log --graph --topo-order --abbrev-commit --date=short --decorate --all --boundary --pretty=format:'%Cgreen%ad %Cred%h%Creset%C(yellow)%d%Creset %s %Cblue[%cn]%Creset'
    last = log -1
    st = status --ignore-submodules -sb
    standup = !"git log --reverse --branches --since='$(if [[ "Mon" == "$(date +%a)" ]]; then echo "last friday"; else echo "yesterday"; fi)' --author=$(git config --get user.email) --format=format:'%C(cyan) %ad %C(yellow)%h %Creset %s %Cgreen%d' --date=local"
    assume = update-index --assume-unchanged
    unassume = update-index --no-assume-unchanged
    assumed = "!git ls-files -v | grep ^h | cut -c 3-"
    accept-ours = "!f() { [ -z \"$@\" ] && set - '.'; git checkout --ours -- \"$@\"; git add -u -- \"$@\"; }; f"
    accept-theirs = "!f() { [ -z \"$@\" ] && set - '.'; git checkout --theirs -- \"$@\"; git add -u -- \"$@\"; }; f"
    review = !git diff @{u}..
